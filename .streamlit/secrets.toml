# ─────────────────────────────────────────────────────────
# DO NOT commit real keys here.
# On Streamlit Community Cloud:
#   App → Settings → Secrets → paste your real values there.
# For local dev, copy this file to .streamlit/secrets.toml
# and fill in real keys (this file is git-ignored).
# ─────────────────────────────────────────────────────────

GROQ_API_KEY    = ""   # https://console.groq.com  (free)
GEMINI_API_KEY  = ""   # https://aistudio.google.com  (optional fallback)
SUPABASE_URL    = ""   # https://supabase.com → project settings → API
SUPABASE_KEY    = ""   # supabase anon/public key
SERPER_API_KEY  = ""   # https://serper.dev  (free 2500 searches/month)
RESEND_API_KEY  = ""   # https://resend.com  (free 3000 emails/month)
